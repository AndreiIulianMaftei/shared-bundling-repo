{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "all"],// run all algorithm
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "self_intersect"],// projected monotonicity
            
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "monotonicity_projection"],// projected monotonicity
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "intersect_all"],// projected monotonicity
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "monotonicity"],// run monotonicity on all algorithm
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "angle"],// run monotonicity on all algorithm

            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "distortion"],// run distortion on all algorithm
            //"args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "frechet"], /// run frechet on all algorithm 
            "args": ["--algorithm", "all", "--folder", "output/airlines", "--metric", "ink"], /// run frechet on all algorithm 
            //"args": ["--algorithm", "epb", "--file", "input_data/airlines.graphml"],  ///run epb algorithm
            //"args": ["--algorithm", "wr", "--file", "input_data/airlines.graphml"],  ///run wr algorithm
            "console": "integratedTerminal"
        }
    ]
}